---
title: 'Getting All Objects in a Store'
type: 'listing'
slug: '/demos/indexeddb/getAll/'
category: 'indexeddb'
summary: 'Retrieve all of the objects in an IndexedDB object store.'
order: 3
section: '5.3'
code:
  - name: 'index.js'
    language: 'javascript'
---

The most basic type of query is one that returns all objects in the object store. This is done by calling `getAll` on the store object while inside a transaction.

Assume you have a database called +users+, with an object store also called +users+.
