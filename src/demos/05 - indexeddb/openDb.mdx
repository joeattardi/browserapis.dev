---
title: 'Creating or Opening a Database
type: 'listing'
slug: '/demos/indexeddb/openDb/'
category: 'indexeddb'
summary: 'Create or open an IndexedDB database.'
order: 1
section: '5.1'
code:
  - name: 'index.js'
    language: 'javascript'
---

Consider the example of a todo list application. It uses an IndexedDB database called `todoList`, and the database has a single object store called `todos`.

To make working with the API a little easier, it has been wrapped in a `Promise`.
