---
title: 'Animating an Element with requestAnimationFrame'
type: 'demo'
slug: '/demos/async/requestAnimationFrame/'
category: 'async'
order: 3
summary: 'Listen for some DOM events to provide a basic drag function to an element.'
---

This demo shows an example of performing a basic animation using the `requestAnimationFrame` API.

Four boxes are animated at different rates of FPS (frames per second).
