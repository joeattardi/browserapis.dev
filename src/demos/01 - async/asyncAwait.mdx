---
title: 'Using the async and await Keywords'
type: 'demo'
slug: '/demos/async/asyncAwait/'
category: 'async'
section: '1.4'
order: 4
summary: 'Use the alternative async/await syntax instead of calling `then` on a Promise.'
---

This demo shows how to interact with `Promise` objects. with the `await` keyword.

It calls a `getUsers` function which simulates a network request, then returns a `Promise` that resolves to the list of users.
