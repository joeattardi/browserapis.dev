---
title: 'Using Promises in Parallel'
type: 'demo'
slug: '/demos/async/promiseAll/'
category: 'async'
section: '1.5'
order: 5
summary: 'Use Promise.all to run several asynchronous operations in parallel.'
---

`Promise.all` takes an array of `Promise` objects and returns a new `Promise` that is fulfilled once all of the given `Promise`s are fulfilled. If any `Promise` rejects, the new `Promise` also rejects.