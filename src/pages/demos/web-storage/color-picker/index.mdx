---
nav:
  key: 'colorPicker'
  parent: 'webStorage'
  label: 'Remembering an Input Value'
  group: 'sidebar'
  order: 1
---
import { MdFullscreen } from 'react-icons/md';
import { Link } from 'gatsby';

export { default } from '../../../../components/DemoLayout';
import CodeBlock from '../../../../components/CodeBlock';
import InlineDemo from '../../../../components/InlineDemo';

import js from '!raw-loader!./index.js';
import css from '!raw-loader!./demo.css';
import html from '!raw-loader!./index.html';

# Remembering an Input Value

Remember a simple input value in local storage.

 - The picker tries to read a color from local storage when the page first loads. If one is found, it's set as the default color.
 - When the color value changes, it is immediately written to local storage.

## Demo

<div className="demo">
  <InlineDemo js={js} css={css} html={html} />
</div>

## Code

<CodeBlock code={js} language="javascript" />
<CodeBlock code={html} language="html" />
<CodeBlock code={css} language="css" />
